[defaults]
inventory = inventory.ini
host_key_checking = False
interpreter_python = auto_silent

[privilege_escalation]
# Always use sudo for become, which is standard on Ubuntu/WSL2
become = True
become_method = sudo
become_user = root

# Force Ansible to actually prompt/wait properly for password on the correct TTY
become_ask_pass = False